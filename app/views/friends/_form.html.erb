<%= render "shared/errors", obj: @friend %>

<table class="attr">
    <tr>
        <th><%= form.label :name %></th>
        <td><%= form.text_field :name, size: 8, :size=>"30x10" %></td>
    </tr>
</table>